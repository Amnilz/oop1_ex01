#include "macros.h"
#include "Vertex.h"
#include "Board.h"
#include "Triangle.h"
#include "Utilities.h"
#include <cmath>


/**
Triangle::Triangle(const Vertex vertices[3])
{
}


bool Triangle::is_valid(const Vertex & v1, const Vertex & v2,const Vertex & v3) const
{
	
	if ((!v1.isValid() || !v2.isValid() || !v3.isValid())) return false;
	double epsilon = 0.1;
	if (abs(v1.m_row - v2.m_row) < epsilon && (abs(v1.m_row - v3.m_row)< epsilon );

}

*/

